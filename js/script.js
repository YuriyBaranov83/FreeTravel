$(document).ready (function () {
   $('.cards').slick({
      infinite: true,
      slidesToShow : 6
   }
   );
});

$(document).ready (function () {
   $('.places').slick({
      // infinite: true,
      slidesToShow : 1
   }
   );
});